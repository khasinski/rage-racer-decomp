/* Section markers that linkers/JAP10/main.ld computes, listed here with the
   address they happen to take in the retail layout.

   This file is NOT linked: main.ld already defines these names, and an
   assignment here would pin them to the retail address and undo the whole
   point of them.  It exists so tools/scripts/dis_sym.py can recognise the
   address while disassembling and emit the name instead of a frozen address,
   and so gen_expected.py can preserve that name in the reference objects. */

main_BSS_END = 0x801F3CA4;
