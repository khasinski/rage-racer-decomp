# Decompilation notes for `func_8003F9C4`

## Scope

This pass is intentionally limited to the 859-word `func_8003F9C4` in
`main/track/GameDrawScriptedScenery.c` and the register-allocation diagnostic
needed to investigate it.

**Do not continue to the other large functions. They will be assigned
separately.**

## Result

`func_8003F9C4` remains an `INCLUDE_ASM` stub. Its full semantic reconstruction
is preserved under `scratch/decomp-work/func_8003F9C4/`, but no candidate is
byte-exact, so replacing the stub would violate the project acceptance rule.

The best byte-position residual improved from `584` to `504`:

| candidate | words | exact | aligned |
|---|---:|---:|---:|
| `candidate-04-control-flow.c` | 856 | 584 | 255 |
| `candidate-codex2-global-record-signed-range.c` | 858 | 568 | 200 |
| `candidate-codex2-tail-frame-pointer.c` | **859** | **504** | 195 |

The 504 candidate also has the retail frame and save set:
`.frame $sp,104,$31`, 80 bytes of locals, and only `$s0` saved.

A second line of investigation produced a much closer instruction shape while
changing register allocation:

| candidate | words | exact | aligned |
|---|---:|---:|---:|
| `candidate-codex2-direct-cursor-phase-first.c` | 863 | 597 | 112 |
| `candidate-codex2-direct-phase-frames-first.c` | 863 | 597 | **108** |

For the 108-aligned candidate the remaining shape differences are isolated to:

| region | aligned differences |
|---|---:|
| position keyframe update | 18 |
| position interpolation | 41 |
| rotation keyframe update | 18 |
| rotation interpolation | **0** |
| distance/audio tail | 31 |

This is evidence about source shape, not a valid conversion: it is four
instructions too long and has 597 byte-position mismatches.

## Exhaustion evidence

The original mechanism-class candidates remain preserved and were remeasured:

| candidate | mechanism | words | exact |
|---|---|---:|---:|
| `candidate-01-temp-lifetime.c` | temporary lifetime | 858 | 588 |
| `candidate-02-rematerialise-address.c` | address rematerialisation | 872 | 725 |
| `candidate-04-control-flow.c` | branch/control-flow spelling | 856 | 584 |
| `candidate-05-storage-view.c` | scalar globals versus cursor structs | 892 | 837 |
| `candidate-07-initialisation-order.c` | cursor initialisation order | 859 | 785 |
| `candidate-08-frame-shape.c` | displacement aggregate versus scalars | 856 | 591 |
| `candidate-09-arithmetic-spelling.c` | unsigned squared-distance shifts | 855 | 586 |
| `candidate-10-register-pins.c` | pinned long-lived cursor bases | 860 | 772 |

Additional focused candidates cover committed-global record addressing,
condition operand order, direct cursor storage, tail statement order, named
frame temporaries, named products, declaration/register pins, multiplication
operand order, and signed versus unsigned range tests. Regressions are retained
in the same scratch directory.

Loop shape does not apply because retail contains no loop back-edge. Volatile
aliasing does not describe this ordinary per-frame RAM and was not used as a
matching fiction.

## Allocation diagnostic

`tools/scripts/alloc_diff.py` compares two candidate compilations. It:

- compiles both with GCC 2.6.3 `-dl -dg` dumps;
- selects only the requested function from multi-function dumps;
- reconstructs `reg_may_share` allocno groups;
- recomputes and verifies GCC allocation priority, including multiword size;
- reports allocation order, hard-register/stack coloring, frame shape, COFF
  local names, and linked retail `exact`;
- optionally preserves the `.s`, `.lreg`, and `.greg` evidence.

It deliberately does **not** claim to recover retail pseudos, priorities, or
allocation order from final machine code. Those compiler-time facts are absent
from the retail executable, so the supported comparison is
candidate-versus-candidate only.

Example for this function:

```sh
tools/scripts/alloc_diff.py func_8003F9C4 \
  scratch/decomp-work/func_8003F9C4/candidate-04-control-flow.c \
  scratch/decomp-work/func_8003F9C4/candidate-codex2-tail-frame-pointer.c
```

That comparison reports `exact 584 -> 504`, an unchanged retail-sized frame,
and the relevant allocation/coloring changes.

## Verification

```text
python -m unittest tools.tests.test_alloc_diff
....
Ran 4 tests
OK
```

No production C was substituted for `func_8003F9C4`, because `exact` is not
zero. This pass stops here and does not proceed to any other large function.
